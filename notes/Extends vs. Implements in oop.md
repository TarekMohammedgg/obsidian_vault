#software #oop 
**`extends`** is used to **inherit a class**, while **`implements`** is used to **inherit interfaces**.


`extends` → gets both **methods + attributes**.
`implements` → must define **all methods**, but **no attributes** are inherited.


implements :must using all things (Attributes and methods ) in the interface class 
