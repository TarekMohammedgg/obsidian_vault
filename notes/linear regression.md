#ml_and_dnn 
is types of supervised machine learning 

algorithm learn from labelled dataset point to find the relationship between input features and the target variable. based on independent input variable. 